<section class="cli-band">
  <div class="bb-container cli-grid">
    <div>
      <div class="t-label">Install the CLI</div>
      <h2>One line. Encrypted from the first byte.</h2>
      <p>Scriptable storage for engineers. It verifies itself, adds <code>bb</code> to your PATH, and uploads ciphertext only.</p>
    </div>
    <div class="terminal-card">
      <div class="terminal-top"><span></span><span></span><span></span></div>
      <code><span>$</span> curl -fsSL https://get.beebeeb.io | sh</code>
    </div>
  </div>
</section>

<style>
  .cli-band {
    padding: 86px 32px;
    background:
      linear-gradient(135deg, oklch(0.2 0.015 70), oklch(0.15 0.012 70)),
      var(--color-ink);
    color: var(--color-paper);
  }

  .cli-grid {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: 56px;
    align-items: center;
  }

  .t-label {
    color: var(--color-amber);
  }

  h2 {
    max-width: 560px;
    margin: 14px 0 16px;
    color: var(--color-paper);
    font-size: clamp(34px, 4vw, 54px);
    letter-spacing: -0.04em;
    line-height: 1;
  }

  p {
    max-width: 480px;
    margin: 0;
    color: oklch(0.78 0.01 80);
    font-size: 16px;
    line-height: 1.65;
  }

  .terminal-card {
    overflow: hidden;
    border: 1px solid oklch(0.36 0.012 70);
    border-radius: 10px;
    background: oklch(0.13 0.012 70);
    box-shadow: 0 24px 60px oklch(0 0 0 / 0.22);
  }

  .terminal-top {
    display: flex;
    gap: 7px;
    padding: 12px 14px;
    border-bottom: 1px solid oklch(0.32 0.012 70);
  }

  .terminal-top span {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: oklch(0.52 0.01 80);
  }

  .terminal-card code {
    display: block;
    padding: 28px;
    color: oklch(0.94 0.005 80);
    font-family: var(--font-mono);
    font-size: clamp(15px, 2.2vw, 22px);
    line-height: 1.4;
    overflow-wrap: anywhere;
  }

  .terminal-card code span {
    color: var(--color-amber);
  }

  @media (max-width: 820px) {
    .cli-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
