---
import Base from '../layouts/base.astro';

/* ── Helper ── */
function fmtEur(n: number) {
  return n.toLocaleString('nl-NL');
}

/* ── Tier data (precomputed to avoid <= in template) ── */
const tiersRaw = [
  { p: 39,    n: 'Early Bee',         tb: '1 TB',   dur: '1 year',  retail: 48,    b: 412, cap: null, left: null,  tag: null,           hot: false, star: false, apiary: false },
  { p: 69,    n: 'Founding Hoarder',  tb: '2 TB',   dur: '1 year',  retail: 96,    b: 487, cap: 500,  left: 13,    tag: 'POPULAR',      hot: true,  star: false, apiary: false },
  { p: 149,   n: 'Founding Hoarder+', tb: '4 TB',   dur: '1 year',  retail: 192,   b: 64,  cap: 200,  left: 136,   tag: 'BEST €/TB', hot: false, star: true,  apiary: false },
  { p: 299,   n: 'Drone',             tb: '8 TB',   dur: '1 year',  retail: 384,   b: 612, cap: 1000, left: 388,   tag: null,           hot: false, star: false, apiary: false },
  { p: 599,   n: 'Queen',             tb: '20 TB',  dur: '1 year',  retail: 958,   b: 78,  cap: 100,  left: 22,    tag: '78% CLAIMED',  hot: false, star: false, apiary: false },
  { p: 1499,  n: 'Hive',              tb: '50 TB',  dur: '1 year',  retail: 2394,  b: 19,  cap: 50,   left: 31,    tag: null,           hot: false, star: false, apiary: false },
  { p: 14990, n: 'Apiary',            tb: '100 TB', dur: '5 years', retail: 23940, b: 4,   cap: 25,   left: 21,    tag: 'PATRON',       hot: false, star: false, apiary: true  },
];

const LOW_STOCK_THRESHOLD = 30;

const tiers = tiersRaw.map(t => {
  const saves = t.retail - t.p;
  const pctOff = Math.round((saves / t.retail) * 100);
  const lowStock = t.left !== null && t.left < LOW_STOCK_THRESHOLD + 1;
  const claimedPct = (t.left !== null && t.cap !== null) ? (100 - (t.left / t.cap * 100)) : 0;
  const savesStr = fmtEur(saves);
  const priceStr = fmtEur(t.p);
  const retailStr = fmtEur(t.retail);
  return { ...t, saves, pctOff, lowStock, claimedPct, savesStr, priceStr, retailStr };
});

/* ── Stretch goals ── */
const stretches = [
  { at: '€250k', label: 'Hardware key (YubiKey + Solo) integration', highlight: false, gift: false, cap: false },
  { at: '€350k', label: 'Self-hosted Beebeeb — open the server source', highlight: false, gift: false, cap: false },
  { at: '€450k', label: 'Tor hidden service onion endpoint', highlight: false, gift: false, cap: false },
  { at: '€500k', label: 'Base goal hit — campaign succeeds', highlight: true, gift: false, cap: false },
  { at: '€650k', label: 'Encrypted collaborative docs (real-time)', highlight: false, gift: false, cap: false },
  { at: '€750k', label: 'BONUS — every backer gets +20% storage. Forever.', highlight: false, gift: true, cap: false },
  { at: '€800k', label: 'A second EU region: Lisbon (4-region redundancy)', highlight: false, gift: false, cap: false },
  { at: '€1M',   label: 'BONUS — every backer gets +50% storage (stacks with €750k = +70% total).', highlight: false, gift: true, cap: false },
  { at: '€1.2M', label: 'Bug bounty program · €100k pool', highlight: false, gift: false, cap: false },
  { at: '20 PB-yrs',  label: 'Storage cap reached — new pledges close even if revenue is open. Cost-discipline, not vibes.', highlight: false, gift: false, cap: true },
];

/* ── Features ── */
const features = [
  { k: 'Web · Drive', v: 'Browser app. Drag, drop, share. Encrypted on your device with WebCrypto.' },
  { k: 'iOS + Android',    v: 'Native apps. Photo backup. Biometric unlock. Open source.' },
  { k: 'Desktop sync',     v: 'macOS, Windows, Linux. Selective sync. Conflict-free.' },
  { k: 'bb CLI + SDK',     v: 'For the engineers. rclone backend. Cron-friendly. Scriptable.' },
  { k: 'Sharing',          v: 'Per-link keys. Password gates. Expiring URLs. Recipients never need an account.' },
  { k: 'Team console',     v: 'Admin, audit log, DPA, SSO/SAML. For when your hobby turns into a business.' },
];

/* ── Schedule ── */
const schedule = [
  { d: 'Q3 2026', e: 'Campaign closes. Backers receive recovery phrases + first credentials.' },
  { d: 'Q4 2026', e: 'Closed beta opens. Backers test all three regions.' },
  { d: 'Q1 2027', e: 'Public 1.0 — desktop apps, mobile apps, full CLI, all stretch goals delivered.' },
  { d: 'Ongoing', e: 'Quarterly transparency reports. Annual third-party audit. Warrant canary.' },
];

/* ── FAQ ── */
const faqs = [
  { q: 'What if you don\'t hit €500k?', a: 'All-or-nothing. Every backer is refunded by Kickstarter. We go back to weekends and try again.' },
  { q: 'What if you hit it but disappear?', a: 'We\'ve open-sourced the clients (already on GitHub). The architecture is documented. Worst case, the community can fork. But we\'re not going anywhere — we live here.' },
  { q: 'Can you hand over my files to a government?', a: 'No. We don\'t have the keys. We can hand over encrypted blobs, but they look like random bytes. We publish a warrant canary.' },
  { q: 'Why not use Proton Drive / Mega / Tresorit?', a: 'They\'re great. They also do not all do real zero-knowledge, are not all EU-jurisdiction, and most are not open source. We wanted all three.' },
  { q: 'Is my pledge a subscription?', a: 'No — your KS pledge is a one-time prepaid term (1 year for most tiers, 5 years for the Apiary patron tier). After that, we auto-renew at standard retail (€7.99 per 2 TB / month, billed yearly) with your locked-in 20% backer discount, valid for life — never expires. We email you 30 days before each renewal. Cancel any time, in two clicks. No surprise charges.' },
  { q: 'What does our retail price actually save me vs. iCloud / Drive / Dropbox?', a: 'Apple iCloud+ 2 TB = €10.99/mo. Google One 2 TB = €9.99/mo. Dropbox Plus 2 TB = €11.99/mo. Beebeeb retail 2 TB = €7.99/mo — cheaper than all of them, with zero-knowledge encryption and EU jurisdiction. With your 20% lifetime backer discount it\'s €6.39/mo. Founding Hoarder pledge: €5.75/mo for the first year, then €6.39/mo for life.' },
];

/* ── Activity feed (simulated) ── */
const activity = [
  { name: 'Markus R.', city: 'Berlin', tier: 'Founding Hoarder', ago: '2 min ago' },
  { name: 'Sofia L.',  city: 'Stockholm', tier: 'Queen', ago: '4 min ago' },
  { name: 'Jan W.',    city: 'Amsterdam', tier: 'Early Bee', ago: '7 min ago' },
  { name: 'Emma K.',   city: 'Helsinki', tier: 'Founding Hoarder+', ago: '11 min ago' },
  { name: 'Luca P.',   city: 'Milan', tier: 'Drone', ago: '14 min ago' },
];

/* ── Social proof ── */
const proofs = [
  '"Finally someone doing ZK right in the EU."',
  '"Backed. Exactly what I\'ve been looking for."',
  '"The architecture doc alone is worth reading."',
];

const shares = ['X', 'Reddit', 'HN', 'Mastodon', 'Email'];
---

<Base
  title="Beebeeb Kickstarter Campaign — Zero-knowledge cloud storage"
  description="Back Beebeeb on Kickstarter. Zero-knowledge encrypted cloud storage, built in the EU by Guus & Bram. Help us ship 1.0."
  path="/campaign"
  noindex={true}
>

  <!-- Nav strip -->
  <div class="ks-nav-strip">
    <div class="ks-nav-inner">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" class="ks-nav-star">
        <path d="M12 2l2.09 6.26L21 9.27l-5 4.87L17.18 21 12 17.77 6.82 21 8 14.14l-5-4.87 6.91-1.01z"/>
      </svg>
      <span class="ks-nav-text">/ kickstarter campaign &middot; live preview</span>
      <span class="ks-nav-spacer"></span>
      <span class="ks-nav-badge">DAY 03 OF 30</span>
    </div>
  </div>

  <!-- Main 2-col layout -->
  <div class="ks-layout">

    <!-- LEFT: Pitch + Story -->
    <div class="ks-left">

      <!-- Pitch video placeholder -->
      <div class="ks-video">
        <div class="ks-video-overlay">
          <div class="ks-video-play">
            <div class="ks-video-triangle"></div>
          </div>
          <span class="ks-video-label">[ pitch video &middot; 02:14 &middot; Guus & Bram + the architecture ]</span>
        </div>
        <div class="ks-video-time">2:14</div>
      </div>

      <!-- Headline -->
      <h1 class="ks-headline">
        Beebeeb &mdash; zero-knowledge cloud storage,<br />
        <span class="ks-headline-accent">built in the EU &mdash; by Guus & Bram.</span>
      </h1>
      <p class="ks-intro">
        We're not pitching another "secure" cloud with marketing-speak. We've built the storage we wanted to use ourselves: files encrypted on your device before they leave it, sharded across three EU datacenters, recoverable only with a key we never see. No US Cloud Act. No surveillance. No master password sitting on someone's server. Help us ship 1.0.
      </p>

      <!-- 01 Why we're doing this -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">01</span>
          <span class="ks-chapter-title">Why we're doing this</span>
          <span class="ks-chapter-line"></span>
        </div>
        <p class="ks-body">
          Hi &mdash; we're <strong>Guus & Bram Langelaar</strong>, two brothers from the Netherlands. This isn't our first build together. <strong>I was 8, Bram was 13</strong>, and he sat me down at the kitchen table and walked me through assembling our first PC &mdash; thermal paste, IDE cables, the whole rite of passage. Everything I know about how machines work started there.
        </p>
        <p class="ks-body">
          Twenty years on, we're still building together &mdash; but with day jobs that fit. <strong>Bram</strong> runs <em>Datablocks</em>, the Netherlands' #1 reseller of factory-recertified enterprise drives &mdash; he knows storage hardware better than anyone we've met, and he's why every byte of Beebeeb sits on a 28TB He-sealed Seagate that would otherwise have been e-waste. <strong>I'm Guus</strong> &mdash; software engineer to the bone, tech-enthusiast, and the one who actually loves writing the systems code at 2am. I do the cryptography, the sharding, the clients. Bram does the iron, the unit economics, and the customer conversations.
        </p>
        <p class="ks-body">
          The big US-based Drive solutions still have backdoors &mdash; for governments, for ad targeting, for "trust & safety" scanning of your private files. We don't. We <em>cannot</em>. Even the EU-branded clouds don't do real zero-knowledge &mdash; they hold a key. That's the only honest answer: if a provider <em>can</em> read your files, eventually someone makes them.
        </p>
        <p class="ks-body">
          We're on Kickstarter because we don't want VC money &mdash; VCs want growth, we want a product that lasts. &euro;500k gets us a year of full-time work, three EU regions live, audited clients, and a real launch. Same two brothers, same kitchen table, bigger build.
        </p>
      </div>

      <!-- 02 What you actually get -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">02</span>
          <span class="ks-chapter-title">What you actually get</span>
          <span class="ks-chapter-line"></span>
        </div>
        <div class="ks-features-grid">
          {features.map(f => (
            <div class="ks-feature-card">
              <div class="ks-feature-key">{f.k}</div>
              <div class="ks-feature-val">{f.v}</div>
            </div>
          ))}
        </div>
      </div>

      <!-- 03 Architecture -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">03</span>
          <span class="ks-chapter-title">The architecture (in one diagram)</span>
          <span class="ks-chapter-line"></span>
        </div>
        <div class="ks-arch-wrap">
          <div class="ks-arch-diagram">
            <div class="ks-arch-center">
              <div class="ks-arch-icons">
                <div class="ks-arch-shield">
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                </div>
                <div class="ks-arch-arrow">
                  <svg width="24" height="12" viewBox="0 0 24 12"><path d="M0 6h20M16 2l4 4-4 4" fill="none" stroke="currentColor" stroke-width="1.5"/></svg>
                </div>
                <div class="ks-arch-regions">
                  {['FRA', 'AMS', 'STO'].map(r => (
                    <div class="ks-arch-region">
                      <span class="ks-arch-region-label">{r}</span>
                    </div>
                  ))}
                </div>
              </div>
              <div class="ks-arch-caption">device-side encryption &rarr; 3-of-5 erasure coding &rarr; 3 EU regions</div>
            </div>
          </div>
        </div>
        <p class="ks-caption-text">
          Files are encrypted on-device with a key derived from your recovery phrase. Encrypted blobs are split into shards (3-of-5 erasure coding) and distributed to Falkenstein, Helsinki, and Ede. We never see plaintext. We never see your key. If we got a court order tomorrow, the answer would be: nothing to hand over.
        </p>
      </div>

      <!-- 04 Stretch goals -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">04</span>
          <span class="ks-chapter-title">Stretch goals</span>
          <span class="ks-chapter-line"></span>
        </div>
        <div class="ks-stretch-list">
          {stretches.map(s => (
            <div class:list={['ks-stretch-item', s.highlight && 'ks-stretch-item--highlight', s.gift && 'ks-stretch-item--gift', s.cap && 'ks-stretch-item--cap']}>
              <span class:list={['ks-stretch-amt', s.highlight && 'ks-stretch-amt--highlight', s.gift && 'ks-stretch-amt--gift', s.cap && 'ks-stretch-amt--cap']}>{s.at}</span>
              <span class:list={['ks-stretch-label', (s.gift || s.cap) && 'ks-stretch-label--bold']}>{s.label}</span>
              {s.highlight && <span class="ks-stretch-badge ks-stretch-badge--base">BASE GOAL</span>}
              {s.gift && <span class="ks-stretch-badge ks-stretch-badge--gift">FREE FOR ALL BACKERS</span>}
              {s.cap && <span class="ks-stretch-badge ks-stretch-badge--cap">HARD CAP</span>}
            </div>
          ))}
        </div>
      </div>

      <!-- 05 Schedule -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">05</span>
          <span class="ks-chapter-title">Schedule</span>
          <span class="ks-chapter-line"></span>
        </div>
        <div class="ks-schedule">
          {schedule.map((row, i) => (
            <div class:list={['ks-schedule-row', i < schedule.length - 1 && 'ks-schedule-row--border']}>
              <span class="ks-schedule-date">{row.d}</span>
              <span class="ks-schedule-event">{row.e}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- 06 FAQ -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">06</span>
          <span class="ks-chapter-title">FAQ</span>
          <span class="ks-chapter-line"></span>
        </div>
        <div>
          {faqs.map(item => (
            <div class="ks-faq-item">
              <div class="ks-faq-q">{item.q}</div>
              <div class="ks-faq-a">{item.a}</div>
            </div>
          ))}
        </div>
      </div>

      <!-- 07 Risks & challenges -->
      <div class="ks-chapter">
        <div class="ks-chapter-header">
          <span class="ks-chapter-num">07</span>
          <span class="ks-chapter-title">Risks & challenges</span>
          <span class="ks-chapter-line"></span>
        </div>
        <p class="ks-body">
          We're realistic. The biggest risk is <strong>not the tech</strong> &mdash; the encrypted-storage tech is already running in our basement. The biggest risk is <strong>support load</strong> if we sign up 20,000 people and only two of us answer email. That's why higher tiers are capped, and why we're prioritizing async docs, devlog transparency, and a community Matrix room over a 24/7 support team we can't staff yet.
        </p>
        <p class="ks-body">
          Second risk: regulatory. We're EU-only on purpose. We won't take US business until we have a US-shaped legal entity, which is not in scope for v1.
        </p>
      </div>
    </div>

    <!-- RIGHT: Pledge Sidebar -->
    <div class="ks-right">
      <div class="ks-sidebar">

        <!-- Funding stats -->
        <div class="ks-funding-amount">&euro;127,840</div>
        <div class="ks-funding-goal">pledged of &euro;500,000 goal</div>

        <!-- Progress bar -->
        <div class="ks-progress">
          <div class="ks-progress-fill" style="width: 25.5%; background: linear-gradient(90deg, var(--amber), var(--amber-deep));"></div>
        </div>

        <!-- Stats row -->
        <div class="ks-stats-grid">
          <div>
            <div class="ks-stat-num">1,284</div>
            <div class="ks-stat-label">backers</div>
          </div>
          <div>
            <div class="ks-stat-num">27</div>
            <div class="ks-stat-label">days to go</div>
          </div>
          <div>
            <div class="ks-stat-num">25%</div>
            <div class="ks-stat-label">funded</div>
          </div>
        </div>

        <!-- CTAs -->
        <a href="#tiers" class="btn btn-amber btn-md ks-sidebar-cta">Back this project</a>
        <a href="#remind" class="btn btn-outline btn-md ks-sidebar-cta">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
          Remind me
        </a>

        <!-- Urgency bar -->
        <div class="ks-urgency">
          <div class="ks-urgency-live">
            <div class="ks-urgency-dot"></div>
            <span class="ks-urgency-live-label">Live</span>
          </div>
          <div class="ks-urgency-text">
            <strong>18 people</strong> backed in the last hour. Founding Hoarder tier has <strong class="ks-urgency-hot">13 spots left</strong>.
          </div>
        </div>

        <div class="ks-divider"></div>

        <!-- Live activity feed -->
        <div class="ks-activity">
          <div class="ks-activity-heading">Recent backers</div>
          <div class="ks-activity-list">
            {activity.map(a => (
              <div class="ks-activity-item">
                <div class="ks-activity-avatar">
                  <span>{a.name.split(' ').map(w => w[0]).join('')}</span>
                </div>
                <div class="ks-activity-info">
                  <div class="ks-activity-name"><strong>{a.name}</strong> &middot; {a.city}</div>
                  <div class="ks-activity-tier">{a.tier}</div>
                </div>
                <span class="ks-activity-ago">{a.ago}</span>
              </div>
            ))}
          </div>
        </div>

        <!-- Storage allocation meter -->
        <div class="ks-storage-meter">
          <div class="ks-storage-header">
            <span class="ks-storage-title">STORAGE ALLOCATED</span>
            <span class="ks-storage-spacer"></span>
            <span class="ks-storage-val">11.4</span>
            <span class="ks-storage-max">/ 20 PB-years</span>
          </div>
          <div class="ks-storage-bar">
            <div class="ks-storage-bar-fill" style="width: 57%; background: linear-gradient(90deg, var(--amber), var(--amber-deep));"></div>
            <div class="ks-storage-bar-cap"></div>
          </div>
          <div class="ks-storage-note">
            We commit a <strong>hard ceiling of 20 PB-years</strong> across the campaign &mdash; that's 60 PB physically (3x redundancy across Falkenstein / Helsinki / Ede). Once allocated, tiers close even before their cap. We don't over-promise storage we can't buy and rack.
          </div>
        </div>

        <!-- Tier heading -->
        <div id="tiers" class="ks-tier-heading">
          <span class="ks-tier-heading-label">SELECT A TIER</span>
          <span class="ks-tier-heading-spacer"></span>
          <span class="ks-tier-heading-note">retail = &euro;7.99 / 2 TB / mo</span>
        </div>

        <!-- Tiers -->
        <div class="ks-tier-list">
          {tiers.map(t => (
              <div class:list={['ks-tier-card', t.hot && 'ks-tier-card--hot', t.star && 'ks-tier-card--star', t.apiary && 'ks-tier-card--apiary']}>
                <div class="ks-tier-price-row">
                  <span class="ks-tier-price">&euro;{t.priceStr}</span>
                  <span class="ks-tier-retail">&euro;{t.retailStr}</span>
                  <span class="ks-tier-price-spacer"></span>
                  {t.tag && (
                    <span class:list={['ks-tier-tag', t.star && 'ks-tier-tag--star']}>{t.tag}</span>
                  )}
                </div>
                <div class="ks-tier-name">{t.n}</div>
                <div class="ks-tier-storage">
                  <strong>{t.tb}</strong> &middot; {t.dur}
                </div>
                <div class="ks-tier-saves">
                  SAVE &euro;{t.savesStr} &middot; {t.pctOff}% OFF
                </div>
                {t.apiary && (
                  <div class="ks-tier-apiary-box">
                    <div class="ks-tier-apiary-label">EVERYTHING IN HIVE, PLUS</div>
                    <div>Hall-of-Founders page on beebeeb.io (your name + photo + bio) &middot; named-contact priority support (direct line to Guus & Bram, not a queue) &middot; yearly studio visit in Amsterdam (coffee + roadmap preview) &middot; 1hr/yr co-design call with Guus & Bram &middot; 90-day early access to every new feature &middot; custom subdomain (yourname.beebeeb.io) &middot; gold engraved hex tile + signed launch print (numbered /25) &middot; signed DPA &middot; 25 seats &middot; admin console &middot; 100 TB pooled across 60 months &middot; 20% lifetime renewal discount.</div>
                  </div>
                )}
                <div class="ks-tier-backers-row">
                  <span class="ks-tier-backers">{t.b} backers</span>
                  {t.left !== null && (
                    <span class:list={['ks-tier-left', t.lowStock && 'ks-tier-left--low']}>
                      {t.lowStock ? '⚠ ' : ''}{t.left} of {t.cap} left
                    </span>
                  )}
                </div>
                {t.left !== null && t.cap !== null && (
                  <div class="ks-tier-progress">
                    <div
                      class:list={['ks-tier-progress-fill', t.lowStock && 'ks-tier-progress-fill--low']}
                      style={`width: ${t.claimedPct}%;`}
                    ></div>
                  </div>
                )}
              </div>
          ))}
        </div>

        <!-- Renewal note -->
        <div class="ks-renewal-note">
          <strong>After your prepaid term:</strong> auto-renew at standard retail (&euro;7.99 per 2 TB / month, billed yearly). Your <strong>20% backer discount stays for life</strong> &mdash; simple, generous, never expires. We email you 30 days before each renewal. Cancel any time, in two clicks.
        </div>

        <div class="ks-divider"></div>

        <!-- Social proof -->
        <div class="ks-social-proof">
          {proofs.map(p => (
            <div class="ks-proof-quote">{p}</div>
          ))}
        </div>

        <!-- Share buttons -->
        <div class="ks-share-heading">SHARE</div>
        <div class="ks-share-btns">
          {shares.map(s => (
            <a href="#" class="btn btn-outline btn-sm ks-share-btn">{s}</a>
          ))}
        </div>

        <div class="ks-divider"></div>

        <!-- All-or-nothing notice -->
        <div class="ks-aon">
          <div class="ks-aon-title">ALL-OR-NOTHING</div>
          <div class="ks-aon-text">
            Your card is only charged if we hit &euro;500k. Otherwise: full refund, no questions, no hard feelings.
          </div>
        </div>

      </div>
    </div>

  </div>

</Base>

<style>
  /* Hide scroll progress on campaign page */
  .bb-scroll-progress { display: none; }

  /* Pulse animation for live indicator */
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  /* ── Nav strip ──────────────────────────────────────────── */
  .ks-nav-strip {
    border-bottom: 1px solid var(--line);
    background: var(--paper-2);
  }

  .ks-nav-inner {
    max-width: 1400px;
    margin: 0 auto;
    padding: 12px 24px;
    display: flex;
    align-items: center;
  }

  .ks-nav-star {
    color: var(--amber);
  }

  .ks-nav-text {
    margin-left: 12px;
    font-size: 12px;
    color: var(--ink-3);
  }

  .ks-nav-spacer {
    flex: 1;
  }

  .ks-nav-badge {
    padding: 4px 10px;
    background: var(--amber);
    color: var(--ink);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    border-radius: 6px;
    font-family: var(--font-mono);
  }

  /* ── Layout ─────────────────────────────────────────────── */
  .ks-layout {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr;
  }

  .ks-left {
    padding: 32px 24px;
  }

  .ks-right {
    padding: 32px 24px;
    background: var(--paper-2);
  }

  .ks-sidebar {
    /* no sticky on mobile */
  }

  /* ── Video placeholder ──────────────────────────────────── */
  .ks-video {
    position: relative;
    height: 320px;
    background: var(--paper-3);
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 20px;
    border: 1px solid var(--line);
  }

  .ks-video-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 12px;
  }

  .ks-video-play {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--paper);
    box-shadow: var(--shadow-2);
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--line-2);
  }

  .ks-video-triangle {
    width: 0;
    height: 0;
    border-top: 12px solid transparent;
    border-bottom: 12px solid transparent;
    border-left: 18px solid var(--ink);
    margin-left: 4px;
  }

  .ks-video-label {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--ink-3);
  }

  .ks-video-time {
    position: absolute;
    bottom: 12px;
    left: 12px;
    padding: 4px 8px;
    border-radius: 4px;
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--paper);
    background: oklch(0.18 0.01 70 / 0.85);
  }

  /* ── Headline / intro ───────────────────────────────────── */
  .ks-headline {
    font-size: 28px;
    line-height: 1.08;
    letter-spacing: -0.03em;
    font-weight: 700;
    margin-bottom: 12px;
  }

  .ks-headline-accent {
    color: var(--amber-deep);
  }

  .ks-intro {
    font-size: 15px;
    color: var(--ink-2);
    line-height: 1.65;
    margin-bottom: 24px;
  }

  /* ── Chapters ───────────────────────────────────────────── */
  .ks-chapter {
    margin-bottom: 32px;
  }

  .ks-chapter-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 12px;
  }

  .ks-chapter-num {
    font-family: var(--font-mono);
    font-size: 11px;
    padding: 2px 6px;
    background: var(--ink);
    color: var(--amber);
    border-radius: 4px;
    font-weight: 600;
    letter-spacing: 0.06em;
  }

  .ks-chapter-title {
    font-size: 19px;
    font-weight: 600;
    letter-spacing: -0.015em;
  }

  .ks-chapter-line {
    flex: 1;
    height: 1px;
    background: var(--line);
  }

  .ks-body {
    font-size: 14px;
    color: var(--ink-2);
    line-height: 1.65;
    margin-bottom: 12px;
  }

  .ks-body:last-child {
    margin-bottom: 0;
  }

  /* ── Features grid ──────────────────────────────────────── */
  .ks-features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
  }

  .ks-feature-card {
    padding: 14px;
    background: var(--paper-2);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .ks-feature-key {
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .ks-feature-val {
    font-size: 12.5px;
    color: var(--ink-2);
    line-height: 1.5;
  }

  /* ── Architecture diagram ───────────────────────────────── */
  .ks-arch-wrap {
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--paper);
    padding: 8px;
    margin-bottom: 10px;
  }

  .ks-arch-diagram {
    height: 260px;
    border-radius: 6px;
    background: var(--paper-2);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ks-arch-center {
    text-align: center;
  }

  .ks-arch-icons {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 16px;
    align-items: center;
  }

  .ks-arch-shield {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    background: var(--amber-soft);
    border: 1px solid oklch(0.86 0.07 90);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--amber-deep);
  }

  .ks-arch-arrow {
    color: var(--ink-3);
  }

  .ks-arch-regions {
    display: flex;
    gap: 8px;
  }

  .ks-arch-region {
    width: 44px;
    height: 56px;
    border-radius: 8px;
    border: 1px solid var(--line);
    background: var(--paper-3);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ks-arch-region-label {
    font-family: var(--font-mono);
    font-size: 9px;
    color: var(--ink-3);
    font-weight: 600;
  }

  .ks-arch-caption {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--ink-3);
  }

  .ks-caption-text {
    font-size: 12.5px;
    color: var(--ink-3);
    line-height: 1.55;
  }

  /* ── Stretch goals ──────────────────────────────────────── */
  .ks-stretch-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .ks-stretch-item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 10px 14px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .ks-stretch-item--highlight {
    background: var(--amber-soft);
    border-color: oklch(0.86 0.07 90);
  }

  .ks-stretch-item--gift {
    background: oklch(0.96 0.04 145);
    border-color: oklch(0.78 0.13 145);
  }

  .ks-stretch-item--cap {
    background: oklch(0.96 0.02 80);
    border-color: var(--ink-3);
  }

  .ks-stretch-amt {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    color: var(--ink-2);
    min-width: 80px;
  }

  .ks-stretch-amt--highlight { color: var(--amber-deep); }
  .ks-stretch-amt--gift { color: oklch(0.42 0.13 155); }
  .ks-stretch-amt--cap { color: var(--ink); }

  .ks-stretch-label {
    font-size: 13px;
    color: var(--ink);
  }

  .ks-stretch-label--bold {
    font-weight: 600;
  }

  .ks-stretch-badge {
    margin-left: auto;
    padding: 2px 8px;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    border-radius: 2px;
    white-space: nowrap;
  }

  .ks-stretch-badge--base {
    background: var(--amber);
    color: var(--ink);
  }

  .ks-stretch-badge--gift {
    background: oklch(0.72 0.16 155);
    color: var(--paper);
  }

  .ks-stretch-badge--cap {
    background: var(--ink);
    color: var(--paper);
  }

  /* ── Schedule ───────────────────────────────────────────── */
  .ks-schedule {
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--paper);
    overflow: hidden;
  }

  .ks-schedule-row {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 16px;
    padding: 12px 16px;
  }

  .ks-schedule-row--border {
    border-bottom: 1px solid var(--line);
  }

  .ks-schedule-date {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--ink-3);
  }

  .ks-schedule-event {
    font-size: 13px;
    color: var(--ink-2);
  }

  /* ── FAQ ─────────────────────────────────────────────────── */
  .ks-faq-item {
    padding: 14px 0;
    border-bottom: 1px solid var(--line);
  }

  .ks-faq-q {
    font-size: 13.5px;
    font-weight: 600;
    margin-bottom: 6px;
  }

  .ks-faq-a {
    font-size: 13px;
    color: var(--ink-2);
    line-height: 1.55;
  }

  /* ── Sidebar ────────────────────────────────────────────── */
  .ks-funding-amount {
    font-variant-numeric: tabular-nums;
    font-size: 36px;
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--ink);
  }

  .ks-funding-goal {
    font-size: 12px;
    color: var(--ink-3);
  }

  .ks-progress {
    margin-top: 10px;
    margin-bottom: 14px;
    height: 6px;
    background: var(--paper-3);
    border-radius: 999px;
    overflow: hidden;
  }

  .ks-progress-fill {
    height: 100%;
    border-radius: 999px;
  }

  .ks-stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin-bottom: 16px;
  }

  .ks-stat-num {
    font-variant-numeric: tabular-nums;
    font-size: 18px;
    font-weight: 700;
    color: var(--ink);
  }

  .ks-stat-label {
    font-size: 11px;
    color: var(--ink-3);
  }

  .ks-sidebar-cta {
    width: 100%;
    justify-content: center;
    font-weight: 600;
    margin-bottom: 8px;
    text-decoration: none;
  }

  /* ── Urgency ────────────────────────────────────────────── */
  .ks-urgency {
    margin-top: 16px;
    padding: 12px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .ks-urgency-live {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 6px;
  }

  .ks-urgency-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--green);
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .ks-urgency-live-label {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink-3);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .ks-urgency-text {
    font-size: 12px;
    color: var(--ink-2);
    line-height: 1.5;
  }

  .ks-urgency-hot {
    color: var(--amber-deep);
  }

  .ks-divider {
    margin: 20px 0;
    height: 1px;
    background: var(--line);
  }

  /* ── Activity feed ──────────────────────────────────────── */
  .ks-activity {
    margin-bottom: 16px;
  }

  .ks-activity-heading {
    font-size: 10.5px;
    font-weight: 600;
    color: var(--ink-3);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-bottom: 8px;
  }

  .ks-activity-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .ks-activity-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 6px 0;
  }

  .ks-activity-avatar {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: var(--paper-3);
    border: 1px solid var(--line);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ks-activity-avatar span {
    font-size: 9px;
    font-weight: 700;
    color: var(--ink-3);
  }

  .ks-activity-info {
    flex: 1;
    min-width: 0;
  }

  .ks-activity-name {
    font-size: 11.5px;
    color: var(--ink);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .ks-activity-tier {
    font-size: 10px;
    color: var(--ink-3);
  }

  .ks-activity-ago {
    font-size: 10px;
    color: var(--ink-3);
    white-space: nowrap;
  }

  /* ── Storage meter ──────────────────────────────────────── */
  .ks-storage-meter {
    margin-bottom: 16px;
    padding: 12px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .ks-storage-header {
    display: flex;
    align-items: baseline;
    gap: 6px;
    margin-bottom: 6px;
  }

  .ks-storage-title {
    font-size: 10.5px;
    font-weight: 600;
    color: var(--ink-2);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .ks-storage-spacer { flex: 1; }

  .ks-storage-val {
    font-family: var(--font-mono);
    font-size: 11.5px;
    font-weight: 700;
    color: var(--ink);
  }

  .ks-storage-max {
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--ink-3);
  }

  .ks-storage-bar {
    position: relative;
    height: 8px;
    background: var(--paper-2);
    border-radius: 4px;
    overflow: hidden;
  }

  .ks-storage-bar-fill {
    height: 100%;
    border-radius: 4px;
  }

  .ks-storage-bar-cap {
    position: absolute;
    right: 0;
    top: -2px;
    width: 1px;
    height: 12px;
    background: var(--ink-3);
  }

  .ks-storage-note {
    margin-top: 8px;
    font-size: 11px;
    color: var(--ink-2);
    line-height: 1.5;
  }

  /* ── Tier selection ─────────────────────────────────────── */
  .ks-tier-heading {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 8px;
  }

  .ks-tier-heading-label {
    font-size: 10.5px;
    font-weight: 600;
    color: var(--ink-3);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .ks-tier-heading-spacer { flex: 1; }

  .ks-tier-heading-note {
    font-family: var(--font-mono);
    font-size: 9.5px;
    color: var(--ink-3);
  }

  .ks-tier-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .ks-tier-card {
    position: relative;
    padding: 12px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .ks-tier-card--hot {
    border-color: var(--amber-deep);
    box-shadow: 0 0 0 3px oklch(0.94 0.06 90 / 0.5);
  }

  .ks-tier-card--star {
    border-color: oklch(0.55 0.12 155);
  }

  .ks-tier-card--apiary {
    border-color: oklch(0.55 0.14 60);
    box-shadow: 0 0 0 3px oklch(0.92 0.06 70 / 0.5);
    background: oklch(0.97 0.02 80);
  }

  .ks-tier-price-row {
    display: flex;
    align-items: baseline;
    gap: 6px;
    margin-bottom: 4px;
  }

  .ks-tier-price {
    font-weight: 700;
    font-size: 15px;
  }

  .ks-tier-retail {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink-3);
    text-decoration: line-through;
  }

  .ks-tier-price-spacer { flex: 1; }

  .ks-tier-tag {
    padding: 2px 6px;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    border-radius: 2px;
    background: var(--amber);
    color: var(--ink);
  }

  .ks-tier-tag--star {
    background: oklch(0.72 0.16 155);
    color: var(--paper);
  }

  .ks-tier-name {
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 2px;
  }

  .ks-tier-storage {
    font-size: 11.5px;
    color: var(--ink-2);
    margin-bottom: 6px;
  }

  .ks-tier-saves {
    display: inline-block;
    padding: 2px 6px;
    border-radius: 2px;
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.02em;
    margin-bottom: 6px;
    background: oklch(0.95 0.05 145);
    color: oklch(0.42 0.13 155);
  }

  .ks-tier-apiary-box {
    margin-top: 4px;
    margin-bottom: 6px;
    padding: 10px;
    background: var(--ink);
    color: var(--paper);
    border-radius: 4px;
    font-size: 11px;
    line-height: 1.55;
  }

  .ks-tier-apiary-label {
    font-family: var(--font-mono);
    font-size: 9.5px;
    color: var(--amber);
    letter-spacing: 0.06em;
    margin-bottom: 4px;
  }

  .ks-tier-backers-row {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .ks-tier-backers {
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--ink-3);
  }

  .ks-tier-left {
    font-family: var(--font-mono);
    font-size: 10px;
    margin-left: auto;
    color: var(--ink-3);
  }

  .ks-tier-left--low {
    color: var(--amber-deep);
    font-weight: 600;
  }

  .ks-tier-progress {
    margin-top: 6px;
    height: 3px;
    background: var(--paper-2);
    border-radius: 2px;
    overflow: hidden;
  }

  .ks-tier-progress-fill {
    height: 100%;
    background: var(--ink-2);
    transition: width 500ms var(--ease);
  }

  .ks-tier-progress-fill--low {
    background: var(--amber-deep);
  }

  /* ── Renewal note ───────────────────────────────────────── */
  .ks-renewal-note {
    margin-top: 10px;
    padding: 10px;
    background: var(--paper);
    border: 1px dashed var(--line-2);
    border-radius: 6px;
    font-size: 11px;
    color: var(--ink-2);
    line-height: 1.5;
  }

  /* ── Social proof ───────────────────────────────────────── */
  .ks-social-proof {
    margin-bottom: 16px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .ks-proof-quote {
    font-size: 12px;
    color: var(--ink-2);
    font-style: italic;
    line-height: 1.5;
    padding-left: 12px;
    border-left: 2px solid var(--amber);
  }

  /* ── Share ──────────────────────────────────────────────── */
  .ks-share-heading {
    font-size: 10.5px;
    font-weight: 600;
    color: var(--ink-3);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-bottom: 8px;
  }

  .ks-share-btns {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }

  .ks-share-btn {
    text-decoration: none;
    font-size: 12px;
  }

  /* ── All-or-nothing ─────────────────────────────────────── */
  .ks-aon {
    padding: 12px;
    background: var(--amber-soft);
    border: 1px solid oklch(0.86 0.07 90);
    border-radius: 6px;
  }

  .ks-aon-title {
    font-size: 10.5px;
    font-weight: 600;
    color: var(--amber-deep);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .ks-aon-text {
    font-size: 12px;
    color: var(--ink-2);
    margin-top: 4px;
    line-height: 1.5;
  }

  /* ── Responsive ─────────────────────────────────────────── */
  @media (min-width: 1024px) {
    .ks-layout {
      grid-template-columns: 1.6fr 1fr;
    }

    .ks-left {
      padding: 32px 36px;
      border-right: 1px solid var(--line);
    }

    .ks-right {
      padding: 32px 28px;
    }

    .ks-sidebar {
      position: sticky;
      top: 0;
      max-height: 100vh;
      overflow-y: auto;
      padding-bottom: 32px;
    }

    .ks-video {
      height: 380px;
    }

    .ks-headline {
      font-size: 36px;
    }

    .ks-arch-diagram {
      height: 320px;
    }
  }

  @media (max-width: 560px) {
    .ks-nav-inner {
      padding: 12px 16px;
    }

    .ks-left,
    .ks-right {
      padding: 24px 16px;
    }

    .ks-headline {
      font-size: 24px;
    }

    .ks-features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
